<!-- markdownlint-disable-file -->
# Documentation session: Findings-led assessment

## Mode

Author

## Scope

Document the findings-led assessment experience, the document-readiness
checklist supplied by the user, and the corresponding architecture, deployment,
and operations behavior.

## Sources reviewed

* README.md
* docs/architecture.md
* docs/deployment.md
* docs/operations.md
* docs/improvement-reporting.md
* docs/document-readiness-checklist.md
* src/shaper/application/assessment.py
* src/shaper/application/document_findings.py
* src/shaper/application/estates.py
* src/shaper/domain/estate.py
* src/shaper/interfaces/cli.py
* src/shaper/interfaces/http.py
* bicep/main.bicep
* prototype/copilot-studio-knowledge-compiler/app.js
* prototype/copilot-studio-knowledge-compiler/index.html
* tests/test_architecture.py
* tests/test_estate_interfaces.py

## Outputs

* docs/features.md
* docs/architecture.md
* docs/deployment.md
* docs/operations.md
* README.md navigation update
* docs/document-readiness-checklist.md

## Content checks

* Feature claims are limited to behavior present in source or tests.
* Planned integrations and calibration are identified as planned.
* The document contains no credentials, customer data, or internal-only URLs.
* Headings, tables, links, and callouts follow repository conventions.
* An AI-assistance disclosure is included for external publication review.

## Supplemental authoring update

The user supplied a document-readiness checklist covering file formats,
structure, dependencies, ambiguity, contradictions, completeness, provenance,
retrieval, and parsing hygiene. The content was incorporated as a standalone
guide with explicit agent-impact explanations, semantic tables, accessible
heading structure, and a README navigation link. The read-only attachment
remained unchanged.

## Supplemental validation

* Documentation structure and link target: Passed
* Content sensitivity review: Passed; no credentials, customer data, or
  internal-only URLs are present
* Accessibility review: Passed for heading order, table headers, descriptive
  link text, and plain-language agent-impact sections

## Drift audit

* Added the per-document finding, impact, evidence, persistence, review, and
  recommendation flow to the architecture.
* Separated per-document discovery reports from the estate-wide assessment and
  post-transformation evaluation contracts.
* Documented the optional `agent_impact` compatibility boundary and the
  rollback constraint created by strict older domain models.
* Corrected readiness dependencies to the workflow state store, Knowledge
  Estate store, malware scanner, and compile worker.
* Corrected the current hosted persistence description: PostgreSQL is
  authoritative for Knowledge Estate records and artifact bytes, while the
  container image directs uploads and legacy releases to mounted Azure Files.
* Added operational monitoring and post-deployment smoke checks for finding
  impact, evidence, and score-free recommendation rationale.
* Replaced the remaining score-calibration terminology with quantitative
  readiness calibration.

## Validation

* Earlier full implementation suite: 175 passed with 79.63% coverage
* Earlier Ruff lint and formatting: Passed
* Earlier strict mypy and schema drift checks: Passed
* Earlier JavaScript syntax and findings-led browser smoke test: Passed
* Current targeted architecture and estate-interface tests: 11 passed
* Current relative Markdown link-target check: Passed across seven files
* Current Git whitespace check: Passed
* npm-based Markdown checks: Unavailable because the repository has no
  `package.json`
* CI-owned documentation end-to-end checks: Pending CI

## Output review and token-estimate update

* Documented the labelled, responsive Before reshaping and After reshaping
  panels used to review each generated artifact.
* Documented the estimated model-usage infographic, including input, output,
  expected total, maximum-use guardrail, confidence, and planned filename.
* Verified both views at desktop and 320-pixel widths with no horizontal page
  overflow. The source panel is keyboard-scrollable and the generated HTML
  preview is sandboxed and has an accessible title.
* Verified the complete transformation-button path with controlled same-origin
  responses. Failed workflows now retain and present their specific bounded
  failure reason.
* Full validation: 177 tests passed; Ruff lint and formatting, strict mypy,
  schema drift, JavaScript syntax, and Git whitespace checks passed.

## Final findings and estimate presentation

* Removed the low, medium, and high reshaping-effort band from the Assess table,
  summary, recommendation rationale, styles, and user documentation. Internal
  compatibility fields remain outside the user experience.
* Consolidated token usage into one stacked bar showing estimated input,
  estimated output, and capacity remaining before the enforced maximum.
* Confirmed the chart has an equivalent accessible label and visible legend,
  and reflows at 320 pixels without horizontal page overflow.
* Reconfirmed the complete approved-transformation browser journey and the
  backend HTTP transformation contract.

## Azure deployment

* Deployed the validated worktree to the existing `rg-shaper-dev` development
  environment in UK South.
* Built and published image digest
  `sha256:5170e98a8ea3eff2c60385a03c393298f51d0a3e3b28a3520060f68d6074febb`.
* Activated revision `ca-shaper-dev--0000036` with 100 percent traffic.
* Verified the revision is healthy, liveness and all readiness dependencies
  pass, and the live workspace serves the `20260912-findings3` browser assets.
* Confirmed the deployed workspace routes unauthenticated access through the
  configured Microsoft Entra sign-in flow.

## Architecture drift review

* Corrected the current deployment boundary to show the application container
  and ClamAV sidecar sharing one Container Apps replica.
* Added a current-development deployment diagram covering Container Apps
  authentication, PostgreSQL, Azure Files, Azure OpenAI, Container Registry,
  managed identity, and Log Analytics.
* Distinguished Knowledge Estate artifact bytes in PostgreSQL from uploads and
  legacy compilation releases in Azure Files.
* Expanded the evaluation architecture from two contracts to three: estate
  assessment, document discovery findings, and deterministic post-transformation
  evaluation.
* Distinguished the executable Knowledge Estate transformation workflow from
  proposal-only platform analysis and the legacy compilation workflow.
* Documented the exact-approval boundary for estimator identity, source and
  recommendation versions, model deployment, and enforced token maximum.
* Documented estimator v1.1 behavior: prompt and schema-derived overhead, one
  bounded repair, safety margin, provider-reported accounting, and pre-provider
  rejection of stale estimates.

## Token-budget correction deployment

* Reproduced the estimator gap against the real shaping prompt and structured
  response schema. The schema alone contains more than 1,200 lexical units,
  compared with the former fixed 300-token allowance.
* Updated estimator v1.1 to derive prompt and schema overhead from the active
  request contract and reserve two bounded model calls with a safety margin.
* Limited Knowledge Estate transformations to the same two calls funded by the
  estimate.
* Added regression coverage for the reported 3,372-token first response and for
  rejecting an approved v1.0 estimate before any provider call.
* Full local validation passed: 178 tests, Ruff lint and formatting, strict
  mypy, schema drift, JavaScript syntax, and Git whitespace.
* Deployed image digest
  `sha256:b29b388ea8c3261c7fe5e39f4dca9e7f689ca1bedc2d571f92f17d32730f3a20`.
* Activated revision `ca-shaper-dev--0000037` with 100 percent traffic.
* Verified healthy liveness and readiness for the state store, estate store,
  malware scanner, and compile worker.
* Verified the live workspace labels the third token-chart segment as the
  repair-and-safety contingency.
* CI-owned documentation end-to-end checks remain Pending CI because this
  repository has no local documentation build toolchain.

## Diagram coverage

* Added the canonical C4 Mermaid notation legend.
* Updated the container diagram with the deployed Malware Scanner and the
  synchronous Azure AI dependency.
* Replaced the informal Azure topology flowchart with an evidence-backed C4
  deployment diagram for the UK South development environment.
* Expanded the component diagram with the Estate Recommendation Service, Estate
  Transformation Service, malware-scanning path, generated-artifact persistence,
  and current Azure Files boundary.
* Added a document-readiness impact diagram showing how source conditions affect
  extraction, retrieval, and agent answers, and how findings support content-owner
  decisions.
* Added direct diagram navigation from the README, deployment guide, and feature
  guide.
* C4 Mermaid source validation: Passed.
* Mermaid CLI render validation: Not run because `mmdc` is unavailable and no
  dependency installation was authorized.
